[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "rbo"
version = "0.1.3"
description = "Local compatibility build of rbo for Human-SIG"
requires-python = ">=3.14"
dependencies = [
    "numpy",
    "tqdm",
]
